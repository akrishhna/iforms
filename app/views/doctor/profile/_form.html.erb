<div class="form">
  <%= form_for @doctor, :url => doctor_profile_path, :html => {:class => "form-horizontal"} do |f|%>
    <fieldset>
      <legend><%= form_header %></legend>
      <div class="control-group">
        <%= f.label :doctorname, :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :doctorname %>
        </div>
      </div>
      <div class="control-group">
        <%= f.label :firstname, :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :firstname %>
        </div>
      </div>
      <div class="control-group">
        <%= f.label :lastname, :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :lastname %>
        </div>
      </div>
      <div class="form-actions">
        <%= f.submit 'Update', :class => "btn btn-primary" %>
      </div>
    </fieldset>
  <% end %>
</div>